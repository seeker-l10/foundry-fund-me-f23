[profile.default]
src = "src"
out = "out"
libs = ["lib"]
ffi = true
# ffi tell foundry to run cmd on our machine

# remappings = ["@chainlink/contracts=lib/chainlink-brownie-contracts/contracts/"]

# See more config options https://github.com/foundry-rs/foundry/tree/master/config
